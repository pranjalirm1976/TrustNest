{
  "builds": [{
    "src": "govtech-guardians/package.json",
    "use": "@vercel/static-build",
    "config": { "distDir": "govtech-guardians/dist" }
  }],
  "routes": [{ "src": "/(.*)", "dest": "govtech-guardians/dist/$1" }]
}
